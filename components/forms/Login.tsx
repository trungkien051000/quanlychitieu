import React from 'react';
import { Text } from 'react-native';

const LoginForm: ILoginFormComponent<ILoginFormComponentProps> = () => {
    return <Text>Login Form</Text>;
};

export default LoginForm;
